export default function DashboardPage() {
  return (
    <div className="rounded-2xl bg-white p-6 shadow-sm sm:p-8">
      <p className="text-xs font-medium tracking-wide text-gray-400">SEEKER</p>
      <h1 className="mt-2 text-2xl font-semibold tracking-tight text-gray-950">
        Your applications
      </h1>
      <p className="mt-3 max-w-xl text-sm leading-6 text-gray-500">
        Application tracking lands in a later task. Use the sidebar to navigate —
        this shell is ready.
      </p>
    </div>
  );
}
